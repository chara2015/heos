heos0.01 official version is completed. Currently, the only things left are the player data migration feature and the whitelist module testing. Then, the webcfg hasn't been started yet. A small test for log filtering has been completed, and more filtering may be added in the future (of course, also auto/log TPS) (oh, and the commands also need to be redone, the syntax is too hard to use).